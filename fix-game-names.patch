--- a/EmuLibrary/PlayniteCommon/StringExtensions.cs
+++ b/EmuLibrary/PlayniteCommon/StringExtensions.cs
@@ -106,6 +106,9 @@
             newName = newName.Replace(''', '\'');
             newName = Regex.Replace(newName, @"\[.*?\]", "");
             newName = Regex.Replace(newName, @"\(.*?\)", "");
+            
+            // Remove common prefixes and qualifiers that might appear in file/folder names
+            newName = Regex.Replace(newName, @"^(patch|update|installer|setup|gog\s*installer|disc\s*\d+|cd\s*\d+)\s+", "", RegexOptions.IgnoreCase);
             
             newName = Regex.Replace(newName, @"\s*:\s*", ": ");
             newName = Regex.Replace(newName, @"\s+", " ");